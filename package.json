{
    "name": "defycommerce",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "dependencies": {
        "@defycommerce/medusa": "workspace:*",
        "@defycommerce/frontend": "workspace:*"
    },
    "scripts": {
        "dev": "lerna run dev --stream",
        "build": "lerna run build --stream",
        "start": "lerna run start --stream"
    },
    "devDependencies": {
        "lerna": "^8.1.8"
    }
}